import 'package:get/get.dart';

class Dimensions {
  static double screenWidth = Get.context!.width;
  static double screenHeight = Get.context!.height;

  // static double heightConst = screenHeight / 932;
  // static double widthConst = screenWidth / 430;
}
